# JavaScript / TypeScript RulePack

Semgrep rules under `rules/javascript/` cover:

- `innerHTML` XSS sinks
- `eval()` usage
- Hardcoded secrets in JS/TS sources

Pair with `eslint_security` and `npm_audit` detectors for dependency and lint coverage.
